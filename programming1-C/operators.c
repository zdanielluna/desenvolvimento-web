/* Operadores Aritméticos - utilizados para realizar operações matemáticas
    +   Adiciona dois operandos.	A + B = 30
    −	Subtrai o segundo operando do primeiro.	A − B = -10
    *	Multiplica ambos operandos.	A * B = 200
    /	Divide numerador pelo denominador.	B / A = 2
    %	Resto da operação após a divisão de dois números.	B % A = 0
    ++	Operador de incremento, incrementa 1 ao valor da variável.	A++ = 11
    --	Operador de decremento, decrementa 1 do valor da variável.	A-- = 9 
*/

/* Operadores Relacionais - utilizados para comparação de valores
    ==	Checa se o valor de A é igual ao valor de B. (A == B) 
    !=	Verifica se A e B possuem valores diferentes. (A != B)
    >	Testa se o valor de A é maior que o de B. (A > B) 
    <	Testa se o valor de A é menor que o de B. (A < B)
    >=	Testa se o valor de A é maior ou igual ao valor de B. (A >= B) 
    <=	Testa se o valor de A é menor ou igual ao valor de B. (A <= B) 
*/

/* Operadores Lógicos - utilziados para testar a veracidade de uma expressão lógica.
    && Operador and, se ambas as expressões lógicas forem verdadeiras o valor final é verdadeiro. (A && B) 
    || Operador or, se uma das expressões for verdadeira o valor final será verdade. (A || B) 
    ! Operador de Negação, inverte o valor lógico. !(A && B) 
*/

/* Operadores de Atribuição
    =	Atribuição simples, atribui o valor do lado direito do sinal para a variável do lado esquerdo. C = A + B 
    +=	Soma o valor de C com o de A e atribui o resultado a C. C += mesma coisa que C = C + A
    -=	Subtrai o valor de A do valor de C e atribui o novo valor a C. C -= mesma coisa que C = C - A
    *=	Multiplica C por A e atribui o produto a C. C *= A mesma coisa que C = C * A
    /=	Divide C por A e atribui o resultado a C. C /= A is equivalent to C = C / A
    %=	Atribui o valor do resto dos operandos.

/* Operadores bit a bit */
