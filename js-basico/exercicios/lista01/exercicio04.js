imprimeDivisao = (dividendo, divisor) =>
  console.log(
    `dividendo ${dividendo} | divisor ${divisor} \nResultado: ${
      dividendo / divisor
    } \nResto: ${dividendo % divisor} `
  )

imprimeDivisao(10, 4)
